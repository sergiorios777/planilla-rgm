# Bitácora de Actividades (30/04/2026):

1. **Exclusión de Regidores**: Modificamos la consulta SQL en `ObtenerContratosActivosPlanilla` para excluir automáticamente los puestos de dietas (`es_dietario = false`) del cálculo de la planilla regular.

2. **Motor Simulador de Costos (Fase 2)**: Construimos la función `SimularCostoMensualPuesto`, capaz de proyectar el costo de la municipalidad (Genérica 2.1) inyectando un contrato ideal sin faltas ni tardanzas.

3. **Generación del PIA (Fase 3)**: Desarrollamos el servicio `GenerarProyeccionPIA` en Go. Este agrupa dinámicamente los ingresos y aportes en una matriz (Meta, Fuente/Rubro y Clasificador) de 12 meses. Resolvimos un error de tipo `VARCHAR(50)` separando el ID de la descripción larga al guardar en la base de datos.

4. **Interfaz Gráfica y HTMX (Fase 4)**: Maquetamos `presupuesto_ui.html`. Implementamos HTMX para generar la proyección en segundo plano y dibujamos la matriz utilizando una tabla responsiva de Pico CSS.

5. **Filtro de Versiones**: Optimizamos la consulta `ObtenerMatrizPorAnio` con una subconsulta SQL para asegurar que la vista siempre muestre la última versión generada del PIA.

6. **Inicio del "Año Vigente"**: Diseñamos la lógica híbrida (`GenerarProyeccionAnioVigente`) y escribimos la consulta `ObtenerGastoRealPorPuesto` para extraer los montos ya pagados desde las tablas `planilla_detalles` y `planillas`.